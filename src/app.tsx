import "./app.scss";
import { Root } from "./root";

export const App = () => {
  // wrapper component that is intended to include contexts
  // if required
  return (
    <>
      <Root />
    </>
  );
};
